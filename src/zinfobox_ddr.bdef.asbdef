projection implementation in class zbp_infobox_dd unique;
strict ( 2 );
use draft;

define behavior for ZINFOBOX_DDR //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action DEPData;
}